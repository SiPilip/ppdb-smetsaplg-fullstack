# ============================================================
#  Template .env.production untuk VPS
#  SALIN file ini ke VPS dan ISI nilainya
#  JANGAN pernah commit file ini ke git!
# ============================================================

# ── MongoDB Credentials ────────────────────────────────────
MONGO_ROOT_USER=ppdb_admin
MONGO_ROOT_PASSWORD=GANTI_DENGAN_PASSWORD_KUAT_DI_SINI
MONGO_DB_NAME=ppdb_smetsaplg

# ── Koneksi MongoDB (otomatis dipakai docker-compose) ──────
# Tidak perlu diubah, sudah terkonfigurasi di docker-compose.yml
# MONGODB_URI dibangun otomatis dari variabel di atas

# ── JWT Secret ────────────────────────────────────────────
# Gunakan password yang sangat panjang dan random!
# Generate dengan: openssl rand -base64 64
JWT_SECRET=GANTI_DENGAN_JWT_SECRET_SANGAT_PANJANG_DAN_RANDOM

# ── Fonnte WhatsApp Token ─────────────────────────────────
FONNTE_TOKEN=ISI_DENGAN_TOKEN_FONNTE_ANDA

# ── URL Aplikasi ──────────────────────────────────────────
# Jika belum punya domain, gunakan: http://<IP_VPS>
# Jika sudah punya domain: http://yourdomain.com atau https://yourdomain.com
NEXT_PUBLIC_APP_URL=http://IP_VPS_ANDA

# ============================================================
#  Catatan Keamanan:
#  1. Permission file ini harus 600: chmod 600 .env.production
#  2. Hanya root/deploy user yang boleh membacanya
#  3. Generate password kuat: openssl rand -base64 32
# ============================================================
